name: 'gcp_etl_dbt'
version: '1.0.0'

profile: 'gcp_etl_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  gcp_etl_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +schema: gold
